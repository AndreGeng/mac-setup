---
alwaysApply: true
---

When working with libraries, frameworks, or APIs, fetch current documentation instead of relying on training data. Prefer exact package names and version-specific documentation, then cite the authoritative source.
