# Global OpenCode Instructions

- Invoke an available skill whenever the task matches its description.
- For features, define the requirement before implementation and verify the result.
- For bugs, establish the root cause before changing code.
- For code changes, keep the diff small, run relevant tests, and inspect Git state before committing.
- Never commit credentials, authentication state, session history, internal endpoints, or private user paths.
