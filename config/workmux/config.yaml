nerdfont: true
agent: claude --permission-mode auto
merge_strategy: rebase
panes:
  - command: <agent>
    focus: true
  - split: horizontal
    command: lazygit
  - split: vertical
    command: pnpm install || npm install; exec $SHELL -l
sidebar:
  layout: tiles
  width: 40
  templates:
    tiles:
      - "{status_icon} {pane_title} {fill} {elapsed}"
      - "{worktree}"
      - "{agent_icon} {fill} {git_stats}"
  tree:
    enabled: true
    group_by: project
