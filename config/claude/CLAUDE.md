# Global Claude Code Instructions

- Keep global behavior project-agnostic; project rules belong in that repository.
- Read the project's `CLAUDE.md` and imported `AGENTS.md` before changing files.
- Use skills and subagents only when they materially help the current task.
- Ask before destructive commands, external writes, commits, pushes, or deployments.
- Never store API keys or private MCP URLs in Claude settings or backups.
