# gstack

When gstack is installed, use its `/browse` skill for web browsing and its workflow skills for planning, review, QA, and shipping.

Never store API keys or private MCP URLs in Claude settings. Read credentials from the shell environment.
